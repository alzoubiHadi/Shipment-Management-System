<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for the app to open any network connection at all. Without
         this, Android blocks socket creation at the OS level with
         "SocketException: Operation not permitted, errno = 1" — this is the
         exact error seen in the release build. The debug manifest
         (android/app/src/debug/AndroidManifest.xml) already had this
         permission for Flutter's own tooling needs, which is why this never
         showed up in `flutter run` (debug), only in the release APK. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Driver GPS location sharing (UC-21/UC-14): foreground-only, no
         background tracking service — the app reads and reports the
         driver's position periodically while the app is open, so
         companies/admins can see it on the live tracking map. COARSE is
         requested alongside FINE because Android may grant only the
         approximate-location permission depending on the user's choice in
         the permission dialog. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <application
        android:label="FMS"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <!-- The server is plain HTTP (no SSL certificate) since it's only
             reachable on the local network during development. Modern
             Android blocks plain HTTP by default in release builds (this is
             why the phone's browser could reach the API but the installed
             app could not) — this flag explicitly allows it. For a real
             production deployment the server should move to HTTPS and this
             flag should be removed. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required by url_launcher (CompanyDetailsPage's "View Trade
             License" button) to open https:// links in an external browser
             on Android 11+, which hides package visibility by default. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
